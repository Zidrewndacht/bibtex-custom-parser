rem Sample vLLM (WSL Docker w/ enabled HW CUDA passthrough) launch command for Qwen3-30B-A3B 4bit-AWQ. Runs up to ~180 parallel requests with the current prompts on a pair of RTX 3090 (48GB total VRAM)
rem This automatically downloads the model on the first load.
rem vLLM strongly recommended for ResearchParça initial classification/verification as it's optimized for heavy continuous batching. Tested >15x faster against llama.cpp (real world, in this very application)
rem Keep machine mostly unnattended during vLLM inference via WSL, or have a separate GPU/iGPU for display on the host, for best performance.
docker run --rm -it --gpus all -e VLLM_ATTENTION_BACKEND=FLASHINFER -v /mnt/host/d/VMs/HuggingFaceCache:/root/.cache/huggingface -p 127.0.0.1:8086:8086 --ipc=host vllm/vllm-openai:latest --host 0.0.0.0 --port 8086 --model cpatonn/Qwen3-30B-A3B-Thinking-2507-AWQ-4bit --tensor-parallel-size 2 --gpu-memory-utilization 0.8 --reasoning-parser deepseek_r1 --max_model_len 65536 --dtype float16 --disable-custom-all-reduce --kv_cache_dtype fp8_e4m3