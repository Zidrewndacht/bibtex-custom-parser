OK  Identify LLM by name from the API
OK  Remove bogus DOI links from papers that lack so.
OK  Change LLM endpoint URL to go to the server itself instead of /v1/chat/completions
        Ensure correct usage on all files.
OK  Use Config from file instead of requiring commandline

OK  Add column for "page count" (to filter out short papers)
OK  Add columns on DB for "verified by":         User        Computer        null
        Also add a tooltip for LLM namme
OK  Add new column to table and/or detail row:
OK      Verified by

OK  Add missing columns to table and/or detail row:
OK        Model name
OK        Page count
    
OK   Redo from scratch verification pass (LLM reads each DB line and checks if the according inferred values are sane)
OK      Use shared functions from globals.py
      
OK    Add support to reasoning models
OK        Save reasoning to DB
OK        Show reasoning in detail row

OK   Fix page count parser for Zotero exports

OK  Add client-side filters:
OK      Instant search keywords
OK            Ensure detail row still stays linked to corresponding article row
OK        Filter out short papers
OK        Filter out offtopic

OK    Add quick stats (totals at the end of the table)
OK        Also add counter of total papers and total filtered papers
OK    Remove signal handler + set_flags, etc. from automate_classification.py
OK    Change "changed by" to icon instead of model name, consistent with "verified by"
OK    Fix bogus table shading after filtering
OK    Fix model_name not showing in GUI
OK    Fix "other defects" not showing in GUI
OK    Fix "datasets" not showing in GUI
OK    Fix instances of computer_graphics instead of computer_vision
OK    Fix sort by 'verified'

OK?   Send to LLM from GUI
OK        Refactor automate, verify .py
OK        Reprocess and verify all?
OK        Reprocess remaining?
OK        Verify remaining;
OK?            Prevent verifying unprocessed papers;
OK        "Classify this paper" from detail row?
OK        "Verify this paper" from detail row?

OK    Fix automate never reaches final status, stuck at 'processed 524/524' papers after finishing.
OK        Do not search inside thinking traces (as it may think about things the given paper doesn't have, etc.)

OK        Fix css
OK        Tell the verifier that "None" is as valid as null.

OK        Finer-grained classes:
                Features:
                        Tracks (short, open, mouse bite, spur, spurious copper, wrong trace space/width)
                        Holes 
                        Missing component;
                        Wrong/misplaced component;
                        Wrong orientation/polarity of component;
                        Solder (Cold solder, bridging, excess solder, pinhole);
                        Cosmetic defects (any manufacturing defect that does not actually affect functionality: scratches, dirt, etc.);
                Techniques: 
                        Classic CV (pattern matching, manually-programmed feature analysis etc. May or may not leverage optimization algorithms, like genetic, PSO, etc.)
                        Classic ML (SVM, Random Forest, K-NN, LVQ, etc.)
                        DL: Standard CNN (classifiers: ResNet, EfficientNet, etc)
                        DL: Region-based CNN (detectors: R-CNNs, EfficientDet, most YOLO versions)
                        DL: Transformer (e.g. ViT, DETR, etc.) or mixed CNN+Transformer (e.g. YOLOv12)
                        Hybrid/Ensemble methods (including ML+DL, classic+DL, not including 'hybrids' of DL+DL)
                
OK      Fix/verify after refactor:
OK              Counters at the end of table
OK              Sort by
        
OK        Stats (calculated after filtering):
                Count of instances of each journal
                Count of instances of each keyword     
                        Multiple keywords per row
                Count of instances of each research area and count
OK              Fix CSS

OK      Read manually-set page count.

OK      Import bibTeX from GUI?
OK              Automatic Dedup by DOI?
    
        Allow discarding a paper from DB in GUI?
                Maybe not, be consistent with the search sentence

OK      Hide unclassified and off-topic server-side
OK      Add year range instead of "older than x years"

Refactor: optimizations for larger >>1000 papers dataset:

OK      Change min page count to server-side;
OK      Change year to server-side;
OK      Change search to server-side;
OK      Change detail row to lazy load;
OK      Reimplement repeated keywords, authors, areas count server-side 

OK      Reimplement full-client-side GH Pages export
                Optimize this version for speed somehow?

OK      Fix table layout dynamic widths (colgroups)
OK      Add loading spinner for table updates

OK      Group features;
OK      Keep colors consistent across charts;
OK      Fix exported HTML charts;
OK      Show "has Other"
OK      Show "has User Comment"
OK      Add missing stats to server-side version. Can't be done client-side because detail row is lazy-loaded.
OK      Add Export to Excel to server-side implementation.

OK      Add sanity check filter: offtopic papers verified as wrong classification:
OK              Instead, just add a "only wrong classifications" filter then sort by offtopic?
                Potential max. 177 false offtopics of 11709?

OK      Fix server-side search not being capable of searching strings that are JSON keys (as they return all rows because they have the key)?
OK              Index JSON contents for fast search

OK      Refactor filtering.js (too large): separate stats.js
        Automatically reset 'verified' after 're-classify this paper' or manual changes.

OK      Fix fixed-position JS columns (e.g. verifier score updating wrong column on client post-XHR);

OK      Fix chart can grow but can't shrink?
OK      Add filter for feature groups (tracks/holes, solder, PCBA), all client-side;
OK      Add journal vs conference chart;

OK      Add PDF attachment capability
OK      Add PDF annotation capability
OK              Autosave annotations
OK              Brand annotator interface

OK      Add Excel export:
                Count of misclassification suspects;
                Bubble charts about...?
                
OK      Add full zip export (static HTML viewer + database + PDF folders)
OK              Implement full backup/restore
OK              Add a fallback empty DB to always allow app to open without data (so restores can be done from Web UI)

OK      Add solder_other, pcb_other, etc. boolean fields;
OK      Add 'search this' buttons on every statistics list item;
OK      Separate journal count from conference count in the stats;

OK	Add tri-state checkbox for off-topic papers;

OK	Add stacked/cumulative charts;

OK	Re-factor JS to remove duplicated code from ghpages.js: keep only actual export-specific code
OK		Refactor filtering.js
OK		Refactor stats.js

OK      Add keyword cloud
OK      Fix filtering/stats generation: unified client-side logic for now.
OK              deprecated fetchDetailRowLists()
OK              deprecated /get_stats
OK              deprecated FTS search

OK      Shockingly massive performance improvement/fixes (>2~5x responsiveness with no functionality loss detected):
OK              Fix layout (col, colspans) reducing browser ambiguity;
OK                        Pass HTML validation;
OK              Refactor filtering.js:
OK                      Fix multiple redundant redraws;
OK                      Get rid of querySelectors();
OL                      Search directly onto hidden cells;
OK                      Extensive caching;
OK              Add CSS media query for weak HiDPI laptops:
OK                      Force-disable eyecandy if density is larger than most desktops (as it's likely a ~4K laptop suffering on battery); Backdrop-filtering alone was mauling the poor UHD 620 on the 4K C630 Yoga ex-Chromebook

OK      Refactor stats.js for smaller functions
OK              Fix cumulative stacked charts not stacking in right order
OK              Fix blurry cloud;
OK              Fix blurry charts on 1.25 DPR displays;
OK              Stop reloading cloud when changing charts
OK              Stop reloading all charts when changing only some of them
OK              Fix lagging when changing charts

Ok      Integrate embedded emoji favicons
OK      Adapt flatten.py to autoflatten latest version of all relevant code (e.g skipping libraries)

OK      Add BibTeX citation on detail row

OK      Add pie labels

OK      Improve detail row layout/CSS

OK      Remember full state through shareable URL:
OK             Add client filters to URL       
OK                      Respect defaults from HTML
OK             Add searchable authors/keywords

OK     Add client sorting to URL
OK                      Fix sorting regression
OK     Add open detail rows to URL

OK	Improve batch tasks from Web GUI;
OK	        Run batches in a separate process
OK		Add no_features and on_topic_implementations batch modes to Web
OK		Auto-verify after classification by default from Web.

OK	Embed fonts (Inter Tight / Twemoji) in HTML export for consistency;

OK	Fix sort by alphabetic fields
OK	Fix sort by last changed date

        Improve ugly/inconsistent default checkboxes
OK                Show negative vs positive checkboxes differently (tri-state) 

OK      Set up vLLM backend via hardware-accelerated WSL2 Docker:
Ok              Deprecate GBNF, make API generic enough for all main engines (llama.cpp, vLLM, TabbyAPI)
OK              Improve batching (8 > ~256 threads), put vLLM to good use: inference time (full classification + verification) for ~14000 papers reduced from ~6 DAYS to ~8h in the same hardware (2x RTX 3090) with only ~20% increased power draw.
OK              Make it 45000 papers instead, from 1974 to 2026.

OK      Deannualize conferences (imperfect, but it's a way harder natural language problem than initially expected -- still classifies the same conference with 2 or 3 different representations, but better than 9 or one for each year...)
                Automatic deannualize conferences on input (from cached DB data or new inference if the conference wasn't previously seen)

OK      Further remove eyecandy that gives too much performance concerns with too little visual changes. Namely removed backdrop-filters, translucent modals and 3D transforms globally since newer Intel Arc Xe iGPU also disliked those.

OK      Add a separate button in main row for logs/traces: 2 columns:
OK              Changelog: matches table layout from off-topic to commented row, most recent on top, make unchanged fields transparent;
OK              LLM Traces: from PDF to type row, alternate background between traces.

        Refactor browse_db.py (too large)?
OK              Getting rid of server-side stats and search already reduced it a bit, still unwieldy at ~2090 lines though;
                Moving SQLite logic to a separate file for DB operations may cause 
                        'sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread. The object was created in thread id 34640 and this is thread id 16976.'
                Moving helpers away is fine, but there aren't many.

OK      Refactor comms.js. Works fine, just need some better mantainability. Low priority.

OK      Add agentic classification: Re-classify misclassifications (score <=6 starting from empty/worst) automatically until consensus. Agent prompt template should include full classification, classifier trace, verifier trace, user trace and should explains an imperfect verifier verified the imperfect classification (so it's not guaranteed that the verifier is correct in saying it was misclassified to start with).
OK              ThreadPool all misclassifications through this same prompt, them after everything completes, run a new, "normal" verify remaining step (which will take all just re-classified papers), and a new re-classify misclassifications with whatever is still left as misclassified, until there's full consensus or user abort.
OK              Do NOT alternate between classification/verification for each paper as this would fuck up prompt prefix caching. Do all tasks of same class at each step.

OK      Change to a single continuous log per paper instead of separate (last inference only) classifier and verifier traces.
OK              Log all user changes, have a count of fields that were user-changed to a value different than the last inference.
OK?             Use new columns for user overrides in the DB, such that user changes don't really replace the original inferred data, cache it by always setting both by model, only overrides by user, always use overrides to render the page
OK?             Change the server-side API for changes: every change from user or model has to be logged. 
OK?             Classifier changes update the main fields (which are logged) and the last-LLM fields (which are not) and zero the count of user-changed fields.
OK?             User changes update the main fields (which are logged) and update the count of user-changed fields by comparing with latest LLM fields (can go up or down - like if user changes set something back to what LLM had decided)

        Better onboarding for both main and Lite:
 OK             Automatic delete placeholder ID 1 when first importing

        Import papers from exactly 2000 (somehow missing?)
        Import new papers from 2026

OK      Implement ResearchParça Lite, a standalone non-domain-specific PDF organizer/annotator instead, so at least the annotator can be used for other research.

        Add multiuser capability (naming users, user-selectable DB) -- good for offline "sneakernet" collaboration;
                Possibly auth for online server hosting? -- low priority

        Low relevance XHR limitations/fixes:
OK?             Update commented&other field displays on save.
                Update PDF icon on first annotation without manual reload - not too trivial as saving is "external" to the table webpage.

OK      Hide history row when a parent is filtered out

OK      Automatic add/remove paywalled icon to papers based on user trace containing 'paywalled' string?

OK      convert "conference" to "inproceedings" on import, fix such existing offline entries from Scopus.

OK      Implement average-of-3 full runs:
                record 3 full sets of fields(last_llm)+history logs
                When asked to classify, classify all three runs in parallel (not sequentially, of course) - same when asking to verify
                Add automatic classify until consensus for this paper (does consensus for all 3 runs - at least 6 inference requests, more if consensus isn't reached for all of then in first verification)
                Change the main set to be the average result of the 3 sets:
                        For display, make it proportionally translucent when it's a mix like YYU, NNU, YUU, NUU; show a solid warning sign instead if it's YYN, YNN, YUN
                        Change history row to have 4 tabs: main (for final averaged/warnings results and user results), set 1, set 2, set 3.

OK      Isolate most domain-specific logic where possible -- long term, complicated refactor with little usefulness for now:
skipped         separate fixed from domain-specific HTML templates;
skipped         add a python file exclusive for the domain-specific details (DB schema, JSON schemas, etc) and change existing scripts to use it accordingly;
stats-only      separate JS for logic that access the domain-specific HTML parts from generic JS
OK              Actual implementation uses configurable YAML instead, probably much better than original proposal
                

OK      Re-enable CSS minification
OK      Verify inference of text fields: zero inferred "other defects" and "model names" is suspicious, might be a regression: not be parsing/transferring correctly.
OK      Prevent changing verification during comments and fix correpsonding XHR updates
OK      Fix broken history layout when amount of inferred colums change.
OK      Automatic CSS width according to domain_config column counts
OK      Convert HTML comments to Jinja2 server-side comments


        Add further 'agentic' functionality:
                Automatic de-annualize conferences on import onto a separate DB field (to be used for more reliable stats generation) 
                        check existing conference names before creating to ensure no two names for the same conference;
                        Also look at the existing model names and research areas, etc. when inferring a new one?
                        Access DB for "memory" of existing conferences so it doesn't deannualize the same conference in different ways somehow.
                Automatic group together synonym keywords
                Potential tools: 
                        PDF parsing (when available), tool collects PDF intro and conclusions to be added to classification/verification
                                might as well just go for PDF full text at this point nowadays instead.
                                ...?

OK      Update prompt templates: fixed base + dynamic domain-specific parts.

OK      Add a viewer for queue manager logs

OK      Add a viewer for meta-stats (3run stats)
OK              Essentially include the corresponding meta scripts in the frontend + LaTeX generators, but how/where?
                      Human compare wouldn't make much sense as it needs 2 separate DBs.

        Add sequential onboarding with LLM usage for prompted domain_config creation?
                Use a complete separate Flask app -- avoids unnecessary changes to perfectly working webapp for something used rarely.
                Start with PCB AOI defaults for manual edit/setup if no LLM available.
                Show the default domain_config.yaml to be edited (except color scheme, set separately) and direct config for LLM server url and API key
                Apply LLM config. If not configured or unavailable, let settings be set manually
                If LLM is available and working:
                        propose 5 color schemes (default + 4 generated ones) and 3 domain_config.yaml column sets, user edits/approves one, then:
                        propose 3 classify_instructions, 3 classify_outputs, 3 sets of few-shot examples, all at once based on the built domain_config;      
                Apply (autostarts main process, which itself finds DB is missing, creates it and shows the table with the placeholder article and import buttons)
                Maybe use a curated list of color schemes instead of generated ones?

        Add optional stats page layout with no domain-specific fields (RP-Lite based?) + enable/disable option in domain_config.yaml

        Add a button in history for agent trace review report: reads the complete log stream (from oldest to newest per set) from all sets and reports issues/explains conflicts across sets, etc.
                Adds a colored entry in the main log with the report but does NOT recalculates main set.